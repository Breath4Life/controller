#include "FreeRTOS.h"

/**
 * @UserInterfaceTask Handle the peripherals related to the user interface (buttons, LEDs, ...), except the LCD screen.
 *
 * @param pvParameters Set to NULL.
 */
void UserInterfaceTask(void *pvParameters);
